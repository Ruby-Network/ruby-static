/**
 * The config
 * @typedef {{}} Config
 */
const aeroPrefix = "/aero/";
const proxyPrefix = "/fetch";
const prefix = "/go/";
const codec = "plain";
const debug = {
    url: false,
    scoping: false,
    src: false
};
 
export {
    aeroPrefix,
    proxyPrefix,
    prefix,
    codec,
    debug
};